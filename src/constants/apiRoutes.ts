// API Routes Constants
// Update these routes when the actual API endpoints are ready

export const API_ROUTES = {
  // Admin API routes
  ADMIN_VOUCHERS: "/api/admin/vouchers",
  
  // Other API routes can be added here
} as const;

